#include <stdio.h>
int main()
{
int res, x, y, i;
printf("dame y\n");
scanf("%d", &y);
printf("dame x\n");
scanf("%d", &x);
if (x == 0)
{
printf("La potencia es 1\n");
}
else
{
res = y;
for (i = 1; i < x; i++)//La correcion es que i empezaba en 0, por lo cual, se multiplicaba una vez de mas, al inicializar i = 1 se arregla este problema.

{
res = res * y;
}
printf("La potencia es %d\n", res);
}
return 0;
}
